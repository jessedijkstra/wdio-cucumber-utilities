machine:
  node:
    version: 6.9.1
  post:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

dependencies:
  cache_directories:
    - "~/.yarn-cache"

  pre:
    - yarn --version

  override:
    - script/update

test:
  override:
    - script/test
